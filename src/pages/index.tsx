import { Formulario } from "@/components/Formulario";

export default function Home() {
  return (
    <div>
      <main className="min-h-screen w-svw bg-white">
        <Formulario/>
      </main>
    </div>
  );
}
